import { VisualizationConfiguration } from './visualization-configuration';

describe('VisualizationConfiguration', () => {
  it('should create an instance', () => {
    expect(new VisualizationConfiguration()).toBeTruthy();
  });
});
